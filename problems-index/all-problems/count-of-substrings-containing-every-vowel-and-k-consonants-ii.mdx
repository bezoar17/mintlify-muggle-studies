---
title: '🧀 Count of Substrings Containing Every Vowel and K Consonants II'
description: '[leetcode link](https://leetcode.com/problems/count-of-substrings-containing-every-vowel-and-k-consonants-ii/description/)'
mode: 'wide'
---
<Tabs>
  <Tab title="Question" icon="circle-question" iconType="regular">
    <Card>
      <p>You are given a string <code>word</code> and a <strong>non-negative</strong> integer <code>k</code>.</p>  <p>Return the total number of <span data-keyword="substring-nonempty">substrings</span> of <code>word</code> that contain every vowel (<code>&#39;a&#39;</code>, <code>&#39;e&#39;</code>, <code>&#39;i&#39;</code>, <code>&#39;o&#39;</code>, and <code>&#39;u&#39;</code>) <strong>at least</strong> once and <strong>exactly</strong> <code>k</code> consonants.</p>   <p><strong class="example">Example 1:</strong></p>  <div class="example-block"> <p><strong>Input:</strong> <span class="example-io">word = &quot;aeioqq&quot;, k = 1</span></p>  <p><br/> <strong>Output:</strong> <span class="example-io">0</span></p>  <p><br/> <strong>Explanation:</strong></p>  <p>There is no substring with every vowel.</p> </div>  <p><strong class="example">Example 2:</strong></p>  <div class="example-block"> <p><strong>Input:</strong> <span class="example-io">word = &quot;aeiou&quot;, k = 0</span></p>  <p><br/> <strong>Output:</strong> <span class="example-io">1</span></p>  <p><br/> <strong>Explanation:</strong></p>  <p>The only substring with every vowel and zero consonants is <code>word[0..4]</code>, which is <code>&quot;aeiou&quot;</code>.</p> </div>  <p><strong class="example">Example 3:</strong></p>  <div class="example-block"> <p><strong>Input:</strong> <span class="example-io">word = &quot;</span>ieaouqqieaouqq<span class="example-io">&quot;, k = 1</span></p>  <p><br/> <strong>Output:</strong> 3</p>  <p><br/> <strong>Explanation:</strong></p>  <p>The substrings with every vowel and one consonant are:</p>  <ul>  <li><code>word[0..5]</code>, which is <code>&quot;ieaouq&quot;</code>.</li>  <li><code>word[6..11]</code>, which is <code>&quot;qieaou&quot;</code>.</li>  <li><code>word[7..12]</code>, which is <code>&quot;ieaouq&quot;</code>.</li> </ul> </div>   <p><strong>Constraints:</strong></p>  <ul>  <li><code>5 &lt;= word.length &lt;= 2 * 10<sup>5</sup></code></li>  <li><code>word</code> consists only of lowercase English letters.</li>  <li><code>0 &lt;= k &lt;= word.length - 5</code></li> </ul> 
    </Card>
  </Tab>
  <Tab title="Solution" icon="lightbulb" iconType="regular" >
    <Card>
      No notes for this problem
    </Card>
  <Expandable title="code">
    <CodeGroup dropdown>
```ruby title="Count of Substrings Containing Every Vowel and K Consonants II" lines
def count_of_substrings(word, k)
    is_vowel = Array.new(128, false)
    freq = Array.new(128, 0)
    vowels = "aeiou"

    # Mark vowels in the is_vowel array
    vowels.each_char { |v| is_vowel[v.ord] = true }

    response = 0
    current_k = 0
    vowel_count = 0
    extra_left = 0
    left = 0

    (0...word.length).each do |right|
        right_char = word[right].ord

        if is_vowel[right_char]
            freq[right_char] += 1
            vowel_count += 1 if freq[right_char] == 1
        else
            current_k += 1
        end

        # Shrink the window if consonant count exceeds k
        while current_k > k
            left_char = word[left].ord
            if is_vowel[left_char]
                freq[left_char] -= 1
                vowel_count -= 1 if freq[left_char] == 0
            else
                current_k -= 1
            end
            left += 1
            extra_left = 0
        end

        # Adjust left pointer to remove extra vowels
        while vowel_count == 5 && current_k == k && left < right && is_vowel[word[left].ord] && freq[word[left].ord] > 1
            extra_left += 1
            freq[word[left].ord] -= 1
            left += 1
        end

        # Count valid substrings
        response += (1 + extra_left) if current_k == k && vowel_count == 5
    end

    response
end
```
    </CodeGroup>
  </Expandable>
  </Tab>
</Tabs>