---
title: '🧩 Remove Linked List Elements'
description: '[leetcode link](https://leetcode.com/problems/remove-linked-list-elements/description/)'
mode: 'wide'
---
<Tabs>
  <Tab title="Question" icon="circle-question" iconType="regular">
    <Card>
      <p>Given the <code>head</code> of a linked list and an integer <code>val</code>, remove all the nodes of the linked list that has <code>Node.val == val</code>, and return <em>the new head</em>.</p>   <p><strong class="example">Example 1:</strong></p> <img alt="" src="https://assets.leetcode.com/uploads/2021/03/06/removelinked-list.jpg" style="width: 500px; height: 142px;" />  <strong>Input:</strong> head = [1,2,6,3,4,5,6], val = 6 <br/> <strong>Output:</strong> [1,2,3,4,5]   <p><strong class="example">Example 2:</strong></p>   <strong>Input:</strong> head = [], val = 1 <br/> <strong>Output:</strong> []   <p><strong class="example">Example 3:</strong></p>   <strong>Input:</strong> head = [7,7,7,7], val = 7 <br/> <strong>Output:</strong> []    <p><strong>Constraints:</strong></p>  <ul>  <li>The number of nodes in the list is in the range <code>[0, 10<sup>4</sup>]</code>.</li>  <li><code>1 &lt;= Node.val &lt;= 50</code></li>  <li><code>0 &lt;= val &lt;= 50</code></li> </ul> 
    </Card>
  </Tab>
  <Tab title="Solution" icon="lightbulb" iconType="regular" >
    <Card>
      No notes for this problem
    </Card>
  <Expandable title="code">
    <CodeGroup dropdown>
```cpp title="Remove Linked List Elements" lines
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {
public:
    ListNode* removeElements(ListNode* head, int val) {
        
        ListNode* current=head;
        if(head==NULL)
        return NULL;
        if(head->next==NULL && head->val==val)
        return NULL;
        while(current!=NULL && current->next!=NULL){
            if(current->next->val==val)
            {
                current->next=current->next->next;
            }
            else{
                current=current->next;
            }
        }
        current=head;
        if(head->next==NULL && head->val==val)
        return NULL;
        while(current->val==val)
        current=current->next;
        return current;
        
    }
};
```
    </CodeGroup>
  </Expandable>
  </Tab>
</Tabs>