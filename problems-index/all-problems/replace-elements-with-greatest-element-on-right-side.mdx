---
title: "🧩 Replace Elements with Greatest Element on Right Side"
description: '[leetcode link](https://leetcode.com/problems/replace-elements-with-greatest-element-on-right-side/description/)'
mode: 'wide'
---
<Tabs>
  <Tab title="Question" icon="circle-question" iconType="regular">
    <Card>
      <p>Given an array <code>arr</code>,&nbsp;replace every element in that array with the greatest element among the elements to its&nbsp;right, and replace the last element with <code>-1</code>.</p>  <p>After doing so, return the array.</p>   <p><strong class="example">Example 1:</strong></p>   <strong>Input:</strong> arr = [17,18,5,4,6,1] <br/> <strong>Output:</strong> [18,6,6,6,1,-1] <br/> <strong>Explanation:</strong>  - index 0 --&gt; the greatest element to the right of index 0 is index 1 (18). - index 1 --&gt; the greatest element to the right of index 1 is index 4 (6). - index 2 --&gt; the greatest element to the right of index 2 is index 4 (6). - index 3 --&gt; the greatest element to the right of index 3 is index 4 (6). - index 4 --&gt; the greatest element to the right of index 4 is index 5 (1). - index 5 --&gt; there are no elements to the right of index 5, so we put -1.   <p><strong class="example">Example 2:</strong></p>   <strong>Input:</strong> arr = [400] <br/> <strong>Output:</strong> [-1] <br/> <strong>Explanation:</strong> There are no elements to the right of index 0.    <p><strong>Constraints:</strong></p>  <ul>  <li><code>1 &lt;= arr.length &lt;= 10<sup>4</sup></code></li>  <li><code>1 &lt;= arr[i] &lt;= 10<sup>5</sup></code></li> </ul> 
    </Card>
  </Tab>
  <Tab title="Solution" icon="lightbulb" iconType="regular" >
    <Card>
      No notes for this problem
</Card>
  <Expandable title="code">
    <CodeGroup dropdown>
```ruby title="Replace Elements with Greatest Element on Right Side" lines
# @param {Integer[]} arr
# @return {Integer[]}
def replace_elements(arr)
  n = arr.size; max_till_now = arr.last; arr[-1]=-1;
  (n-2).downto(0).each { |i| 
    val = arr[i]
    arr[i] = max_till_now
    max_till_now = [max_till_now, val].max
  }
  arr
end
```
    </CodeGroup>
  </Expandable>
  </Tab>
</Tabs>