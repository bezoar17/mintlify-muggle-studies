---
title: "🧀 Map of Highest Peak"
description: '[leetcode link](https://leetcode.com/problems/map-of-highest-peak/description/)'
mode: 'wide'
---
<Tabs>
  <Tab title="Question" icon="circle-question" iconType="regular">
    <Card>
      <p>You are given an integer matrix <code>isWater</code> of size <code>m x n</code> that represents a map of <strong>land</strong> and <strong>water</strong> cells.</p>  <ul>  <li>If <code>isWater[i][j] == 0</code>, cell <code>(i, j)</code> is a <strong>land</strong> cell.</li>  <li>If <code>isWater[i][j] == 1</code>, cell <code>(i, j)</code> is a <strong>water</strong> cell.</li> </ul>  <p>You must assign each cell a height in a way that follows these rules:</p>  <ul>  <li>The height of each cell must be non-negative.</li>  <li>If the cell is a <strong>water</strong> cell, its height must be <code>0</code>.</li>  <li>Any two adjacent cells must have an absolute height difference of <strong>at most</strong> <code>1</code>. A cell is adjacent to another cell if the former is directly north, east, south, or west of the latter (i.e., their sides are touching).</li> </ul>  <p>Find an assignment of heights such that the maximum height in the matrix is <strong>maximized</strong>.</p>  <p>Return <em>an integer matrix </em><code>height</code><em> of size </em><code>m x n</code><em> where </em><code>height[i][j]</code><em> is cell </em><code>(i, j)</code><em>&#39;s height. If there are multiple solutions, return <strong>any</strong> of them</em>.</p>   <p><strong class="example">Example 1:</strong></p>  <p><strong><img alt="" src="https://assets.leetcode.com/uploads/2021/01/10/screenshot-2021-01-11-at-82045-am.png" style="width: 220px; height: 219px;" /></strong></p>   <strong>Input:</strong> isWater = [[0,1],[0,0]] <br/> <strong>Output:</strong> [[1,0],[2,1]] <br/> <strong>Explanation:</strong> The image shows the assigned heights of each cell. The blue cell is the water cell, and the green cells are the land cells.   <p><strong class="example">Example 2:</strong></p>  <p><strong><img alt="" src="https://assets.leetcode.com/uploads/2021/01/10/screenshot-2021-01-11-at-82050-am.png" style="width: 300px; height: 296px;" /></strong></p>   <strong>Input:</strong> isWater = [[0,0,1],[1,0,0],[0,0,0]] <br/> <strong>Output:</strong> [[1,1,0],[0,1,1],[1,2,2]] <br/> <strong>Explanation:</strong> A height of 2 is the maximum possible height of any assignment. Any height assignment that has a maximum height of 2 while still meeting the rules will also be accepted.    <p><strong>Constraints:</strong></p>  <ul>  <li><code>m == isWater.length</code></li>  <li><code>n == isWater[i].length</code></li>  <li><code>1 &lt;= m, n &lt;= 1000</code></li>  <li><code>isWater[i][j]</code> is <code>0</code> or <code>1</code>.</li>  <li>There is at least <strong>one</strong> water cell.</li> </ul>   <p><strong>Note:</strong> This question is the same as 542: <a href="https://leetcode.com/problems/01-matrix/description/" target="_blank">https://leetcode.com/problems/01-matrix/</a></p> 
    </Card>
  </Tab>
  <Tab title="Solution" icon="lightbulb" iconType="regular" >
    <Card>
      All cells around a water cell have to have a height of 1, then cells around 1 have to have 2, and so on.
Multi-source bfs starting from water cells.
</Card>
  <Expandable title="code">
    <CodeGroup dropdown>
```ruby title="Map of Highest Peak" lines
# @param {Integer[][]} is_water
# @return {Integer[][]}
def highest_peak(is_water)
  m = is_water.size; n = is_water[0].size
  # initially 0-land, 1-water
  # multisource bfs from water cells, to use same input as final result and visited array
  # we can update water to 0, land to -1, and -1 would indicate unvisited cell
  # height for neighbor is 1+current_cell, and water already has 0
  
  queue = []
  (0...m).each do |i|
    (0...n ).each do |j|
      is_water[i][j] == 1 ? (is_water[i][j]=0; queue<<[i,j]) : (is_water[i][j]=-1)
    end
  end
  directions = [[0,1],[0,-1],[1,0],[-1,0]]
  neighbors=->(x,y) {
    directions.map{|dx,dy|
      ((x+dx).between?(0,m-1) && (y+dy).between?(0,n-1) && is_water[x+dx][y+dy]==-1) ? [x+dx, y+dy] : nil
    }.compact
  }

  until queue.empty?
    level_cells = queue
    queue = []

    level_cells.each do |i,j|
      neighbors.call(i,j).each do |n_i, n_j|
        is_water[n_i][n_j] = is_water[i][j]+1
        queue << [n_i,n_j]
      end
    end
  end

  is_water
end
```
    </CodeGroup>
  </Expandable>
  </Tab>
</Tabs>