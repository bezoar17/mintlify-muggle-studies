---
title: '🧩 Find N Unique Integers Sum up to Zero'
description: '[leetcode link](https://leetcode.com/problems/find-n-unique-integers-sum-up-to-zero/description/)'
mode: 'wide'
---
<Tabs>
  <Tab title="Question" icon="circle-question" iconType="regular">
    <Card>
      <p>Given an integer <code>n</code>, return <strong>any</strong> array containing <code>n</code> <strong>unique</strong> integers such that they add up to <code>0</code>.</p>   <p><strong class="example">Example 1:</strong></p>   <strong>Input:</strong> n = 5 <br/> <strong>Output:</strong> [-7,-1,1,3,4] <br/> <strong>Explanation:</strong> These arrays also are accepted [-5,-1,1,2,3] , [-3,-1,2,-2,4].   <p><strong class="example">Example 2:</strong></p>   <strong>Input:</strong> n = 3 <br/> <strong>Output:</strong> [-1,0,1]   <p><strong class="example">Example 3:</strong></p>   <strong>Input:</strong> n = 1 <br/> <strong>Output:</strong> [0]    <p><strong>Constraints:</strong></p>  <ul>  <li><code>1 &lt;= n &lt;= 1000</code></li> </ul> 
    </Card>
  </Tab>
  <Tab title="Solution" icon="lightbulb" iconType="regular" >
    <Card>
      No notes for this problem
    </Card>
  <Expandable title="code">
    <CodeGroup dropdown>
```elixir title="Find N Unique Integers Sum up to Zero" lines
defmodule Solution do
  @spec sum_zero(n :: integer) :: [integer]
  def sum_zero(0), do: []
  def sum_zero(n) when rem(n,2) == 0, do: [n,-n] ++ sum_zero(n-2)
  def sum_zero(n) when rem(n,2) == 1, do: [0] ++ sum_zero(n-1)

  # implementation below also works, implementation above is recursion with base cases
  
  # def sum_zero(n) do
  #   if rem(n,2) == 1, do: [0] ++ sum_zero(n-1), else: Enum.map(1..trunc(n/2)//1, &(&1)) ++ Enum.map(1..trunc(n/2)//1, &(-&1))
  # end
end
```
    </CodeGroup>
  </Expandable>
  </Tab>
</Tabs>